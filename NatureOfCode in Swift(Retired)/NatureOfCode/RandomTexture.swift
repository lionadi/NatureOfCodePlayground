//
//  RandomTexture.swift
//  NatureOfCode
//
//  Created by Adrian Simionescu on 02/10/14.
//  Copyright (c) 2014 Adrian Simionescu. All rights reserved.
//

import Foundation
/*
/// NOTICE: This does not work. Work in Progress
public class RandomTexture
{
    var width : CGFloat = 0;
    var height : CGFloat = 0;
    var randomTextureData : CCRenderTexture!;
    var textureSprite : CCSprite!;
    var drawPixels : CCDrawNode!;
    
    init(width : Int32, height: Int32)
    {
        self.width = CGFloat(width);
        self.height = CGFloat(height);
        self.randomTextureData = CCRenderTexture.renderTextureWithWidth(width, height: height) as CCRenderTexture;
        self.drawPixels = CCDrawNode();
        self.textureSprite = self.GetSprite();
        
        
    }
    
    func GetSprite() -> CCSprite
    {
        self.randomTextureData.beginWithClear(255, g: 255, b: 255, a: 255);
        self.drawPixels.visit();
        for( var x = 0; x < Int(self.width); x++)
        {
            //for( var y = 0; x < Int(self.height); y++)
            
              //  self.drawPixels.dra(CGPointMake(CGFloat(x), CGFloat(50)), radius: 1, color: CCColor.purpleColor());
            //CCShader(
        }
        
        
        
        self.randomTextureData.end();
        
        return CCSprite.spriteWithTexture(self.randomTextureData.sprite.texture) as CCSprite;
    }
}*/